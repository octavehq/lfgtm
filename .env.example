# Development only: copy to .env (gitignored) to point the asset-manager scripts
# at a local octave-artifacts instance instead of production.
# Marketplace installs never have a .env — they always use https://link.octavehq.com.
ARTIFACTS_URL=http://localhost:3015
